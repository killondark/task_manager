## 1. Окружение Ruby

- [x] 1.1 Создать локальный `.tool-versions` в корне проекта со строкой `ruby 3.3.1` и убедиться, что `ruby -v` в корне показывает 3.3.1

## 2. Каркас Rails 8

- [x] 2.1 Сгенерировать Rails-приложение `rails new .` с базой SQLite и стандартной JS-сборкой (importmap + Turbo + Stimulus), убедиться, что структура приложения создана и `bin/rails -v` показывает Rails 8
- [x] 2.2 Собрать зависимости `bundle install` и убедиться, что они устанавливаются без ошибок

## 3. База данных

- [x] 3.1 Выполнить `bin/rails db:prepare` и убедиться, что SQLite-база создана (в папке `db/` появился файл базы, например `development.sqlite3`)
- [x] 3.2 Запустить `bin/rails db:test:prepare` (или эквивалент для RSpec) и убедиться, что тестовая база готова без ошибок

## 4. RSpec

- [x] 4.1 Добавить гем `rspec-rails` в группу тестов в `Gemfile` и выполнить `bundle install`
- [x] 4.2 Выполнить `bin/rails g rspec:install` и убедиться, что созданы `.rspec` и `spec/rails_helper.rb`

## 5. Корневая страница и Hotwire

- [x] 5.1 Создать контроллер `HomeController` с action `index` (без модели) и соответствующий маршрут `root` в `config/routes.rb`
- [x] 5.2 Создать шаблон `app/views/home/index.html.erb` с контентом приложения и убедиться, что Turbo и Stimulus подключены (стандартная сборка Rails 8)
- [x] 5.3 Написать request-тест: GET `/` возвращает статус 200, и smoke-проверить, что приложение отвечает

## 6. Проверка

- [x] 6.1 Запустить `bundle exec rspec` — все тесты проходят
- [x] 6.2 Запустить `bin/rails server` (короткая проверка), убедиться, что корневая страница отдаёт 200, затем остановить сервер